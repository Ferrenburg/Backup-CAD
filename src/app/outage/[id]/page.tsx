export default function OutageBoardIndex() {
  return (
    <div className="flex h-full flex-col items-center justify-center px-6 text-center">
      <p className="text-fg-muted">Select a call from the list, or press N to log a new one.</p>
      <p className="mt-1 text-xs text-fg-dim">Press ? for the keyboard shortcut reference.</p>
    </div>
  );
}
